# План развития «Фабрики гипотез»

Ядро (детерминированная ветка А + LLM-ветка Б) готово и оттестировано. Дальше — веб-сервис
и точечные улучшения. Принцип прежний: одна крепкая фича > кучи сырых; детерминизм в
рассуждении, LLM только в понимании текста (с цитатным гейтом).

## ✅ Сделано (кратко)
- Веб-практики (`--web`) — ДЕТЕРМИНИРОВАННО, без LLM, с гейтом качества (не заголовки), RU-соседи.
- Досье OpenAlex (`--dossier`) — статьи + цитируемость + фраза-причина, без LLM/ключа.
- Единый клиент внешних вызовов `client.py` + телеметрия (`run_metrics.json`), circuit-breaker.
- OCR-предобработка `imageprep.py` (grayscale/автоконтраст/апскейл).
- Эксперимент = тест/метрика/критерий; roadmap (лаб→пилот→внедрение); litsupport (цитаты корпуса);
  feedback (ре-ранк эксперта); export (JSON/CSV/Jira/PDF/DOCX); report_kb (HTML ветки Б).
- Отчёт: чистка воды/эмодзи, источник+бейджи прогона, метрики внизу, ховер, провенанс до ячейки.
- Materials → `data/` (state) / `knowledge/` (reference); preflight LLM/OCR (без зависаний).

---

## 1. Веб-сервис на FastAPI  ← ДЕЛАЕМ СЕЙЧАС
Локальный инстанс (НЕ публичный) для тестирования жюри. Обёртка над готовым ядром;
LLM-части деградируют мягко через preflight. Ставится: `uv sync --extra web`.

### Архитектура (плоская, слоями) — папка `webapp/`
```
webapp/
  main.py            фабрика приложения (роутеры, static, templates, lifespan)
  config.py          настройки + ЛИМИТЫ (размер/типы файлов, локальность)
  di.py              провайдеры зависимостей (интерфейс → реализация)
  interfaces/        Protocol-контракты: LLMClient, SearchClient
  adapters/          реализации: yandex_llm, ddg_search, yandex_search (заглушка под замену)
  infra/             storage (run_id-папки), uploads (multipart, split data/knowledge)
  services/          RunService (оркестрация пайплайна), ReportService (чтение результатов)
  api/schemas.py     pydantic-контракты запрос/ответ
  api/routes/        health, runs, files, pages (Jinja)
  templates/         index (drag-drop), run (карточки), card (деталь)
  static/            app.css, app.js (htmx + ванильный JS)
```

- [ ] **Интерфейсы + DI:** `LLMClient`/`SearchClient` как Protocol; адаптеры Yandex/DDG;
      заглушка `yandex_search` (чтобы заменить DDG на Yandex Search API одним провайдером).
      Проброс через FastAPI `Depends` (Dishka — опциональный апгрейд до контейнерного DI).
- [ ] **Изоляция прогонов:** каждый запуск → `outputs/runs/<run_id>/` (загрузки в `sources/`,
      отчёты/контекст/метрики рядом). Ничего в БД — эфемерно, локально.
- [ ] **Роуты:** `POST /api/runs` (загрузка+KPI+флаги → run_id), `GET /runs/{id}` (карточки),
      `GET /runs/{id}/hyp/{rank}` (деталь), `GET /runs/{id}/sources/{path}` (открыть исходник),
      `GET /health` (доступность LLM/OCR/поиска — preflight).
- [ ] **Стартовый экран:** drag-n-drop с разделением **данные/знания**, поле KPI, ограничения
      (учитываются: «без нового оборудования» — детерминированно в intent.py; богатый
      свободный текст — LLM-трек), чекбоксы `--dossier/--web`.
- [ ] **Карточки:** главная — компактные карточки гипотез (не хламить); клик → деталь.
      Всё прокликиваемо; клик по цитате/ячейке → исходник открывается в новой вкладке
      НА НУЖНОМ МЕСТЕ (`sources/<file>#page=N`).
- [ ] **Визуал:** Jinja2 рендерит (графы — inline SVG), интерактив — htmx + ванильный JS.
- [ ] **Docker:** Dockerfile + compose; локальный запуск `uvicorn`.
- [ ] **Ограничения (написать):** локальный инстанс, не публичный; данные фабрик наружу не
      уходят (только термины запроса к подтверждённым источникам); нет аутентификации/
      многопользовательской изоляции по безопасности (только по run_id); лимиты на размер загрузки.

### Позже по сервису
- Async-клиент (сейчас sync), health-check до приёма запроса (частично есть).
- Длинное извлечение → фон + прогресс через SSE.
- Dishka-контейнер вместо `Depends`, если понадобится сложный DI.

## 2. Надёжный поиск: Yandex Search API
DDG бот-блокирует по IP (в песочнице 202). Заглушка-адаптер `yandex_search` уже под это —
подставить ключ и провайдер, DDG останется fallback'ом.

## 3. Критик (LLM-разбор гипотезы) — когда доступен LLM
- [ ] `critic.py`: риски/слабые места/что проверить, БЕЗ цифр (числа — за метриками).
- [ ] Судейская рубрика: якоря шкалы («3» vs «5») против ~4.4 всем.

## 4. Производительность
- [ ] Параллелить загрузку страниц web/OpenAlex (ThreadPoolExecutor) — демо ≤3 мин
      (сейчас ~87 c на фабрику из-за последовательных запросов).

## 5. OCR — сквозной прогон
- [ ] Скан → текст → извлечение, когда будет OCR-роль ключа Yandex (предобработка готова).

## Совсем потом
- H3: предсказательная модель крупность↔извлечение (6 точек/фабрику — мало).
- Лёгкий агент-оркестратор.

---

## User-фичи: визуальные (частично закрываются веб-сервисом)
- [ ] Интерактивный граф: зум/пан, подсветка соседей по клику (SVG + JS).
- [x] Раскрываемые карточки вместо простыни — через карточки+деталь в вебе.
- [x] Прокликиваемые источники до места — `sources/<file>#page=N` в новой вкладке.
- [ ] Разложение приоритета мини-баром (почему #1 > #2).
- [ ] Экспертная настройка весов ранжирования (ползунки) — кейс просит.

---

## Итерация правок по фидбэку (веб-сервис) — ✅ СДЕЛАНО

Приоритет: корректность (1,2) → доверие к источникам (3) → единый визуал (8,9) →
экспорт (10) → мелочи UI (4,5,6,7).

- [x] **1. Заземление трека Б на элементы фабрики + KPI.** LLM-трек предлагает золото,
      когда в схеме Cu/Ni. Причина: `discover._all_as_direct` добирает связи по новизне
      БЕЗ проверки релевантности → всплывают офф-топик металлы из литературы.
      Фикс: в `run_service._branch_b` строить query из элементов фабрики (имена+символы из
      схемы, НЕ хардкод) + KPI + диагностика; в `discover.discover` требовать
      relevance>0 при доборе, приоритет — пересечение с элементами фабрики. Файлы:
      `webapp/services/run_service.py`, `factory/discover.py`, `factory/reader.py` (имена элементов).

- [x] **2. Граф и кривая раскрытия по ВСЕМ элементам (не только Ni).** Когда из хвостов
      извлечены два элемента без указания в KPI — панели строятся только по `target_element`.
      Фикс: `panels_html`/`analyze` вызывать на каждый `elements_built`, рендерить набор
      панелей (заголовок с символом) или общий граф со всеми элементами. Файлы:
      `webapp/services/run_service.py`, `factory/analysis.py`, `factory/knowledge.py`, `run.html`.

- [x] **3. Кликабельные ссылки на конкретную цитату.** У трека А в «Использованной
      литературе» есть страницы, но нет ссылок; у мировой практики и статей — цитата без
      прямой ссылки на предложение. Фикс: ссылки на источник с якорем текста-фрагмента
      (`#:~:text=<цитата>` для веб-URL; `#page=N` для PDF-файлов), и файл трека А тоже
      кликабелен. Файлы: `card.html`, `lit_card.html`, `factory/export.py`, `report.py`.

- [x] **4. Загруженные материалы — уместить максимум в фиксированный блок.** Свёрнутый
      список ок, но в начальный (нераскрытый) блок вместить как можно больше файлов и дать
      «показать все» разворотом. Файл: `run.html` (info-block), `app.css`.

- [x] **5. Убрать «(+ddg фолбэк)» — показывать только фактически использованный бэкенд.**
      Файлы: `run.html`/`index.html` health/конфиг, `webapp` health, `di.py`/адаптеры (имя).

- [x] **6. Убрать блок «Общее» из «Экспорт и технические детали» полностью.** Файл: `run.html`.

- [x] **7. Стилизовать тумблеры.** Убрать жирный шрифт, прижать вправо, ховер. Файлы:
      `index.html` (`.flags/.sw`), `app.css`.

- [x] **8. Единый визуал трека Б = трек А.** Сейчас блоки/структура расходятся, режет глаз.
      Привести карточки и разделы `lit_card.html`/трек-Б-секцию `run.html` к тем же классам
      и вёрстке, что у трека А (`card.html`, `.rcard`). Файлы: `lit_card.html`, `run.html`, `app.css`.

- [x] **9. Граф трека Б в стиле Obsidian.** Точки с подписями, не слипаются, перетаскиваются
      (force-directed раскладка + drag). Позже — рёбра «точка → гипотеза». Файлы:
      `factory/report_kb.py` (`_graph_svg` → раскладка), `app.js` (drag узлов, анти-overlap).

- [x] **10. Экспорт как аккуратный HTML + трек Б в экспорт.** Текущий PDF/DOCX хуже вида
      страниц и слепляет текст; трек Б не экспортируется. Фикс: экспортировать блочно
      (визуальные разделители, как на странице), добавить экспорт карточек трека Б. Файлы:
      `factory/export.py`, роут экспорта, `run.html`.
